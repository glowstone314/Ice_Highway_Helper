获取出发坐标与目的地坐标
如果不限定1.40625角度，则使用线段函数
如果限定，使用四舍五入计算出最接近的限定角度，使用tan获取冰块位置
由于角度被限定，实际目的地与冰道终点可能会有偏差
最坏情况下，行进1304米即可产生16米的偏移
可以通过向下取整、向上取整获取最接近的两个限定角度，分成两段

生存实装冰道时，由于无法使用命令修改船的偏航角度
只能使用退可入的辅助瞄准功能
但由于mc使用浮点数存储角度信息
有些在日常生活中能整除的角度在游戏里会丢失精度
省事的办法是等分为2^n份